# ASAP
The official repository of ASAP: Advancing medical volumetric representation learning with anatomy-aware semantically-adaptive pre-training



```
conda create -n asap python==3.11.10
pip install 
pip install --no-deps -r requirements.txt
```